# CO-14 — OWNER-TOPO-01 Registration Record (E03/E05/E06 topology decision intake)

**DISPOSITION:** `PENDING_SIGNATURE`. This record registers the adjudicated, signable
OWNER-TOPO-01 decision text for the owner's signature. **No signature exists yet; nothing in
this record executes.** Every "executes" row below is `EXECUTES-ON-SIGNATURE` and stays inert
until the owner signs; every still-gated item stays gated after signature. Posture unchanged:
`venue_environment=OFF / venue_activation=OFF / paper_activation=OFF / shadow_activation=LIVE`,
`DENIED_SAFE_HOLD`.

**Decision file received + cross-checked (2026-08-13).** The change-orders doc opened CO-14 as a
*reserved slot* because the decision file was "not received in this session's upload set." The file
`TOPOLOGY-DECISION-E03-E05-E06.md` has now been received and cross-checked clause-by-clause against
the registered text below: every material clause corresponds — E03=Kairos (Forecast Core,
`decision_authority = NONE`); E05=standalone Coordinator (the only mediator, never re-fuses prose,
never admits/sizes/authorizes); E06=Logos (sole target-state governed-LLM caller,
`decision_authority = NONE`); Kairos and Logos never call/import/discover/depend on each other;
E07/E08/E09 remain the sole admission/risk/venue owners; additive (supersedes no RC2/RC3/RC4 byte);
**source registration + DARK/shadow wiring only**; the 12,000 ms Logos deadline as law with the
25.99 s observation a named violation; the §14 prohibited claims stay prohibited. The three
precision amendments A1–A3 (§2) NARROW, never widen, the received text; the approver
(`leesbak@gmail.com`) and posture (`OFF/OFF/OFF/LIVE / DENIED_SAFE_HOLD`) match. **No signature
exists; the record stays `PENDING_SIGNATURE`.**

**Order:** `TRIAD-ORIGIN-V7-CHANGE-ORDERS-2026-08-12` CO-14 (the reserved slot, now filled by
the received decision file). **Source of the signable text:**
`TRIAD-OWNER-TOPO-01-ADJUDICATION-2026-08-12` (adjudicator: Claude, executive-lead seat per
R32 — adjudication is audit work, not a ceremony seat; **Claude never signs, never arms**).
Adjudication verdict: **SIGN — with three precision amendments applied** (A1, A2, A3 below);
no substantive defect found.

---

## 1. Constitutional check (CO-14 step 2) — PASSED per the adjudication

The adjudication's cross-check (12 material claims against the corpus) found **no authority
leakage**: `decision_authority = NONE` on E03 and E06; E05 explicitly non-admitting /
non-sizing / non-authorizing; the money line (E07/E08/E09) untouched. Anything granting
E05/Logos decision, risk, or execution authority would be **rejected on sight** — nothing in
the finalized text does. The CO-14 step-2 constitutional check is recorded **PASSED** on the
adjudication's authority; this registration adds no independent attestation of its own.

**NOT_ATTESTED register (carried verbatim from the adjudication — open at signature time):**
- Approver-identity ↔ trust-registry equality (Amendment A3): `leesbak@gmail.com` appears
  nowhere else in the supplied corpus; it must equal the pinned trust-registry owner entry
  byte-for-byte **before** signing, or whichever of the two is wrong is corrected first.
- Current PR head states for TriadIntelligence #300 / Logos #6: provider snapshots in the
  corpus are dated 2026-08-11; heads must be reverified before the gate-#4 merges.

## 2. The three amendments (precision only; no substantive change)

- **A1 — "DARK wiring" ambiguity closed:** the decision authorizes **registration and
  declaration only** (registry rows, contract vendoring, wiring-intent declarations in
  deployment class `DARK`) and **no process execution of E05, Kairos, or Logos in any
  deployment class**. (`DARK` here is the gate-#12 deployment class — legitimate post-RC4
  vocabulary; the retired RC4 "connected-dark" *lever* vocabulary is not reintroduced.)
- **A2 — E04 exclusion clause added:** E04 remains excluded from the seam until its E02
  lineage, producer contract, E05 consumer rules, and closure evidence are separately
  registered; no E04→E05 contract exists and none is authorized.
- **A3 — approver identity bound to the trust registry:** the identity must be the exact
  account-owner entry in the pinned trust registry that `--authority` verification (Formula
  Repair §C.2) checks; sign by externally pinned digest, record the pin in the decisions
  register, and confirm byte-for-byte equality before signing.

## 3. Finalized decision text — quoted verbatim from the adjudication §3 (amendments A1–A3 applied)

> **DECISION ID:** `OWNER-TOPO-01`
> **DATE (UTC):** `2026-08-12T09:42:15Z` *(update to actual signing moment)*
> **APPROVER (account owner):** `leesbak@gmail.com` *(must equal the pinned trust-registry owner entry — Amendment A3)*
> **SCOPE:** additive intelligence-plane topology only — no money-line change, no activation-lever change, no supersession of any RC2/RC3/RC4 byte.
>
> I ratify, as an **additive** architecture decision, the mapping:
> - **E03 = Kairos** — Forecast Core. Emits forecast + uncertainty + quality + lineage **only**. No admission, risk, size, policy, venue, or order authority. `decision_authority = NONE`.
> - **E05 = Coordinator** — a **standalone** service that sequences the configured API calls `Origin(E02 output) → E05 → Kairos → E05 → Logos → governed LLM runtime → Logos → E05 → E07`. It validates, attaches, hashes, and envelopes; it **never** re-fuses Logos prose into an authoritative number and **never** admits, sizes, or authorizes. It has no candidate-selection authority; W09 is unchanged; the E07 join binds the advisory bundle to the current W09-selected candidate and rejects identity/lease/revision mismatches.
> - **E06 = Logos** — Language/Knowledge. The **sole target-state** caller of the governed LLM runtime. Returns advisory language + contradiction evidence **only**. `decision_authority = NONE`.
> - **Kairos and Logos never call, import, discover, or depend on each other.** E05 is the only mediator. No silent fallback from Kairos to a legacy forecast route, from Logos to a legacy judge, or from E05 to a direct model call; any alternative route is a new, pinned, testable policy revision.
> - **E04 remains excluded from the seam** until its E02 lineage, producer contract, E05 consumer rules, and closure evidence are separately registered; no E04→E05 contract exists and none is authorized here. *(Amendment A2)*
> - **E07 remains the sole admission/intent owner; E08 the sole risk/size owner; E09 the sole venue actor.** The advisory bundle cannot bypass E07/E08/E09.
>
> This decision authorizes **registration and declaration only**: registry rows, contract vendoring across the train (closure-gate #2), and wiring-intent declarations in deployment class `DARK`. It authorizes **no process execution of E05, Kairos, or Logos in any deployment class**, no deployment, no live listener, no restart, no MCP-family enablement, no PAPER/TESTNET/LIVE, and no venue effect. *(Amendment A1)*
> Preserved posture: `venue_environment=OFF / venue_activation=OFF / paper_activation=OFF / shadow_activation=LIVE / DENIED_SAFE_HOLD`.
> The §14 prohibited claims of the topology correction remain prohibited after signature, including "the canonical bridge is live," "E05 is deployed," "byte-identical," "four valid responses prove reliability," and "the ops relays are private." The 12,000 ms Logos peer deadline stands as law; the observed 25.99 s maximum is a named violation to engineer against, never a tolerance to widen.
>
> **Signature / external pin:** `[owner signature or externally pinned digest — Claude never signs]`

**Signature status in this record: EMPTY / PENDING_SIGNATURE.** The slot above is quoted
as-received; it is filled only by the owner, only after the A3 identity check.

## 4. Registration steps (CO-14 steps 1–4) — each `EXECUTES-ON-SIGNATURE`

| # | CO-14 step | Status |
|---|---|---|
| 1 | Read the decision rows; map each onto the topology correction's 16 closure gates and the CO-03 step-7 contract stubs (`feature_snapshot`, `forecast_request/response.v2`, `logos_prompt_packet.v1`, `logos_explanation.v1`, `logos_contradiction_report.v1`, `e05_intelligence_bundle.v1`) | `EXECUTES-ON-SIGNATURE` |
| 2 | Reject on sight anything granting E05/Logos decision, risk, or execution authority (constitutional boundary) | check **PASSED** on the current text (§1); the rejection rule stays standing law for any future revision |
| 3 | Register ratified rows; open the E05 transform contract as the seam's binding law — the §7 lineage schema: transport-byte hash, source/normalized canonical-JSON hashes, canonicalization/normalization policy IDs, declared transformations, preserved `missing_inputs`/`degraded_mode`; **the `actionable` field renames or is quarantined** (see §5) | `EXECUTES-ON-SIGNATURE` |
| 4 | Latency law: the **12,000 ms** Logos peer deadline stands; the observed **25.99 s** diagnostic maximum is a **named violation to be engineered against, not a tolerance to widen silently** | `EXECUTES-ON-SIGNATURE` (the law itself is already stated in the decision text) |

**What signature cannot execute (each its own later gate, per the adjudication §4):** the
#300/#6 merges (gate #4, exact-head CI + independent adversarial review under GOV-01 —
audit ≠ ceremony); Kairos listener/model registry (#5); Logos composition/prompt
registry/governed runtime (#6); E05 deployment with identity/TLS/externalized
routes/secrets/idempotency/kill switch (#7); Origin ingress + typed E07 publisher + W09 join
(#8); transform registration evidence (#9); adversarial suites (#10); legacy LLM-egress
retirement with network-level denial proof (#11); deployment registry (#12); fresh
attestations (#13); relay hardening — bearer auth is not a private boundary (#14); SHADOW
soak (#15); and **any posture transition ever** (#16 — a separate signed bounded promotion).

## 5. The `actionable` → `conviction_threshold_met` rename note

The seam's authority-like field **`actionable`** must not be vendored into the contract train
under that name: it reads as an instruction to act, which the advisory plane may never emit.
Intake condition riding closure-gate #2: the field **renames to a neutral, exactly-defined
term — the topology correction proposes `conviction_threshold_met` — or is quarantined**;
the contract train refuses to vendor the old name. The renamed field is a *measurement
statement* (a declared threshold was met), never an admission/size/act signal — E07 alone
owns admission regardless of its value.
