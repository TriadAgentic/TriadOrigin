# 09 · Decision Disposition Register

_Reconciled for B00R generation 2 (2026-08-11). This is a **disposition register**, not a questionnaire
deferred to B10: every row names the milestone/gate it must close before, its decision owner,
its current status, its evidence, and its failure behavior while open. Per the operator's
standing directive, rows are **not** asked in chat; they are batched here for the operator. An
explicit decision to keep a value `NOT_RATIFIED` is a closed planning answer and an open
activation blocker — it never authorizes a substitute._

Legend — **Status:** `OPEN` (undecided), `DEFAULT_IN_FORCE` (build proceeds on the recorded
default; operator may veto), `CLOSED` (operator decided / evidence sealed), `BLOCKED_EXTERNAL`
(needs an act outside this session). **Failure behavior** = what the build/runtime does while
the row is open (always fail-closed, never a silent substitute).

## A · Governance & repository

| Row | Question / decision | Required before | Owner | Status | Disposition & evidence | Failure behavior while open |
|---|---|---|---|---|---|---|
| A1 | Repository home ("triad agent" spoken vs `TriadAgentic/TriadOrigin` written) | B00C receipt | Operator | DEFAULT_IN_FORCE | Build proceeds in `TriadAgentic/TriadOrigin` (org read as "TriadAgentic"); all merged work lives there | Relocation would be a fresh operator directive + migration plan |
| A2 | ADR-005 supersession signature (RC4 lever law ratification) | B05 four-plane merge (artifact); any non-OFF activation (signature) | Operator | DEFAULT_IN_FORCE | RC4 + the 2026-08-09 build directive treated as ratification **for DARK implementation only**; B05 prepares the ADR supersession artifact for signature | No non-OFF activation is representable; baseline stays `OFF/OFF/OFF/LIVE` |
| A3 | Historical receipts + forward receipt law | G2 root receipt | Operator countersign; build session seals | DEFAULT_IN_FORCE | R00/B00/B00C/B01–B07 and generation-1 B00R bytes remain immutable audit artifacts. Their self-integrity does not override `B00_B07_INVALIDATION_MANIFEST.v1.json` or `B00R_GENERATION_LEDGER.v1.json`. G2 and every later accepted milestone use one source PR followed by one separate post-merge evidence-only receipt PR. | No historical receipt becomes authenticated or closure-capable by reinterpretation; next source remains frozen until the predecessor receipt merge/anchor validates |
| A4 | Generation-2 CODEOWNERS bootstrap, branch ruleset, and provider rejection proof | Before G2 corrective source merge | Operator (repo admin) | BLOCKED_EXTERNAL | The separate bootstrap is complete: `@djordi10` approved exact head `47706a55…` and PR #35 merged ordinarily as `ba495ba90…`; `main` and `b00r-ruleset-canary` are reserved there. Current live ruleset state is `NOT_ATTESTED` through the available connector. A privileged owner-side GET must precede any change; then install or update the exact `{main,b00r-ruleset-canary}`, empty-exclusion, integration-bound `test-and-verify` / `15368`, no-bypass, merge-only profile and preserve a rejected canary plus rule suite. Historical ruleset `20641102`/PR #33 is not current proof. Issue #5 stays open. | Any intervening main merge, later-only provider evidence, bypass, successful canary update, non-merge integration, or synthetic capture fails; every closure path stays nonzero |
| A5 | Checkpoint ceremony scope | B10 | Operator | DEFAULT_IN_FORCE | TriadOrigin-local checkpoint only (`checkpoint/<date>-origin-v7-build` + `CHECKPOINTS.md` here); estate-wide checkpoint is G9's | No sibling-repo checkpoint branches are created |
| A6 | Estate/operator lanes (361 ESTATE + 57 OPERATOR ledger rows built elsewhere) | G-1 onward (Track B) | Operator | DEFAULT_IN_FORCE | Partition matches "wire with the other surfaces later"; this repo carries contract shapes + ingress expectations only | Track B gates stay `NOT_PASSED`; completion claims stay repository-scoped |
| A7 | Control-plane homing (comparator / authority-fact verifier / legacy bridge / replay runner) | B01 merge (was); disposition re-affirmed for B07 | Operator | DEFAULT_IN_FORCE | Built here as pure, dark, import-isolated libraries. Per the alignment audit: **no admission/arbitration** — `authority_fact_verifier` verifies externally issued `candidate_authority.v1` + technical-writer lease only; never admits candidates, issues leases, or selects money authority | Any admission-shaped API is a build defect; process deployment stays an estate decision |
| A8 | B00R generation-2 root closure: authenticated decisions/trust registry, corrective source, live branch/tag rulesets, negative canary, exact source/receipt reviews, new receipt, and `B00R_RECEIPT_ANCHOR_G2` | B00R G2 close (blocks B01C onward) | Operator (owner + repo admin + independent reviewer + receipt signers) | BLOCKED_EXTERNAL | Generation 1 remains `MERGED_UNVERIFIED`. G2 uses policy v2, the externally pinned G2 registry plus all three `DEC-*-002` decisions, four canonical bootstrap/source PR+review records, exact source provider=Git time, receipt exact-head CI/review and two-parent live-main proof, pre-receipt-merge tag-rule chronology, and the append-only G2 receipt/anchor. PR #33 is reference evidence only. | B00R stays `BLOCKED`; B01C stays frozen until the physical G2 anchor validates; `DENIED_SAFE_HOLD`, OFF/OFF/OFF/LIVE remains invariant |

## B · Activation values (BLOCKING_OWNER_DECISION — stay `NOT_RATIFIED`, fail closed)

All eight ship as `NOT_RATIFIED` registry rows; `require()` refuses them; runtime fails closed.

| Row | Parameter | Required before | Owner | Status | Failure behavior while open |
|---|---|---|---|---|---|
| B1 | PAR-070 `PRODUCTION_RISK_BUDGET` (bps-of-NAV **and** absolute quote cap) | G9 | Operator | OPEN | No production sizing representable; F20 catalog `DENY` |
| B2 | PAR-072 `PRODUCTION_GROSS_EXPOSURE_CAP` | G9 | Operator | OPEN | Same |
| B3 | PAR-076 `MAX_DAILY_NET_LOSS` (stop, UTC reset, realized/unrealized, rel+abs caps) | G7 | Operator | OPEN | No canary window may open |
| B4 | PAR-077 `MAX_PEAK_TO_TROUGH_DRAWDOWN` (+ HWM reset governance) | G7 | Operator | OPEN | Same |
| B5 | PAR-118 `CANARY_CAPSULE` (exactly one semantic version + parameter digest) | G7 | Operator | OPEN | Same |
| B6 | PAR-119 `CANARY_INSTRUMENT` (one venue-qualified instrument) | G7 | Operator | OPEN | Same |
| B7 | PAR-120 `CANARY_SIDE` (one side enum) | G7 | Operator | OPEN | Same |
| B8 | PAR-121 `CANARY_ACCOUNT` (isolated account/risk cell + position mode + permissions) | G7 | Operator | OPEN | Same |

## C · Research decisions (BLOCKING_RESEARCH_DECISION — interfaces + named abstention only)

| Row | Parameter | Required before | Owner | Status | Failure behavior while open |
|---|---|---|---|---|---|
| C1 | RC3-PAR-STRUCT-001 `EQUAL_LEVEL_MAX_SPAN` | B03 **result emission** (interface/refusal may land) | Research + Operator ratify | OPEN | F06 emits the named abstention `F06_UNAVAILABLE_MAX_SPAN_NOT_RATIFIED`; equal-level structures and dependent capsules `SAFE_HOLD` |
| C2 | RC3-PAR-STRUCT-002 `BOOK_TILT_MIN_QUOTE_DEPTH` | B04 result emission | Research + Operator | OPEN | F17 tilt emits its named abstention; dependent capsules `SAFE_HOLD` |
| C3 | RC3-PAR-STRUCT-003 `PROTECTED_SWING_REDUCER_VERSION` | B03 result emission (F08); B06 CHOCH-capsule availability | Research + Operator | OPEN | F08 has **no semantic implementation** (RC3: none authorized); state stays `UNINITIALIZED`; F09 emits accepted breaks with BOS/CHOCH labels withheld (`UNCLASSIFIED_STRUCTURE_STATE_UNAVAILABLE`) |

## D · Proposed parameter bundle (123 × `PROPOSED_RC2_MUST_RATIFY`)

| Row | Decision | Required before | Owner | Status | Disposition | Failure behavior while open |
|---|---|---|---|---|---|---|
| D | Ratify the RC2 proposal bundle wholesale for SHADOW operation, selectively, or keep symbolic until trial registration | B06 candidate emission (consumed subset); G2 (full) | Operator | OPEN — DEFAULT_IN_FORCE for build | Carried verbatim as a status-labelled, digest-pinned **proposal bundle**; tests exercise formulas with explicitly-labelled test vectors; `require()` refuses `PROPOSED_*` in any non-test context | No semantic result emission on a proposed value; machines fail closed or emit named abstention |

## E · Specification conflicts (dispositioned per RC3/RC4 precedence — confirm or veto)

| Row | Conflict | Required before | Status | Disposition |
|---|---|---|---|---|
| E1 | ADR-005 "no testnet ever" vs RC4 four-plane law | B05 | DEFAULT_IN_FORCE | RC4 wins (later owner directive): TESTNET is a canonical isolated venue environment; sibling-repo estate rules untouched |
| E2 | RC1 `*_ns` fields vs UTC-microsecond law | B01 (closed in code) | DEFAULT_IN_FORCE | UTC Unix **microseconds**; ns remainder carried separately |
| E3 | int64 ticks vs base-10 string wire encoding | B01 (closed in code) | DEFAULT_IN_FORCE | Semantic int64 ticks; JSON wire base-10 string |
| E4 | F02 ATR window inclusion (BLK-RC2-007) | B03 semantic merge | DEFAULT_IN_FORCE | RC3 formula text + GV-004 pin the window: the 14 TRs strictly before the evaluation origin (the most recent finalized bar is TR_{x−1}) |
| E5 | F04 fractal tie-break (BLK-RC2-009) | B03 semantic merge | DEFAULT_IN_FORCE | Strict extreme; ties reject (GV-006) |
| E6 | Capsule family naming (BLK-RC2-011) | B06 candidate emission | DEFAULT_IN_FORCE | **No guessed ordinal↔semantic map** (audit P0): B06 implements the explicit semantic-ID registry (`structures/capsules.py`, five stable IDs incl. `dc_swing_bos_first_retest.v1`); every RC2 `CAP01…CAP05` ordinal spelling is refused (`CapsuleUnavailableError`), never accepted as an alias — full per-row binding evidence in register row E21 |
| E7 | Candidate lifecycle abstention/withdrawal (BLK-RC2-013) | B06 | DEFAULT_IN_FORCE | RC3 lifecycle graph; withdrawal is a first-class transition |
| E8 | F18 stop-side `abs()` defect (BLK-RC2-014) | B06 | DEFAULT_IN_FORCE | Directional stop-side invariant; one-tick vectors |
| E9 | W06 cluster-ID circularity (BLK-RC2-012) | B06 | DEFAULT_IN_FORCE | Frozen earliest prospective root ordered by `(availability, candidate_id)`; never re-root |

| E10 | F09 break-predicate scope: the implementation evaluates BOTH directional predicates against every confirmed frozen level (up_break iff C≥L+b, down_break iff C≤L−b — the literal formula text; first-breach dedup by `(level_id, direction)`), not a kind-scoped single predicate. Both readings satisfy GV-008 and the mirror vectors | B03 semantic merge (implemented; confirm or veto) | Operator/Research | DEFAULT_IN_FORCE | Documented in `structures/structure_state.py`; a kind-scoped veto is a one-line predicate change + re-vector |
| E11 | F05 rolling extreme has NO linked RC3 golden vector (`BLOCKED_MISSING_LINKED_GOLDEN_VECTOR`); its exact values are pinned by repository tests only | G2 | Research + Operator | OPEN | Repo tests pin warm-up boundary + current-bar exclusion; the linked RC3 vector remains owed |
| E12 | F06 span-law boundary is INCLUSIVE (span ≤ max_span joins, per PAR-009); an intended strict bound would be a formula-row spec change | B03 (implemented per PAR-009) | Operator | DEFAULT_IN_FORCE | `typed_level_registry.py` + boundary tests |

| E13 | F16 OFI has no linked RC3 golden vector and no timestamp field on the declared book-update input, so PAR-057's max-age window cannot be applied structurally; the parameter is fetched/validated fail-closed but age-filtering is deferred to a future producer-side envelope revision | G2 | Research + Operator | OPEN | `flow_atoms.py` pins its own hand-worked vector; the sequence/watermark/min-updates laws are enforced now |
| E14 | F12 causal order block: RC3's PAR-047/PAR-161 boundary-rule texts state slightly different tie-break wording (latest-event-then-source-id vs latest-qualifier-then-notional-then-source-id); implemented the fuller PAR-161 rule (notional, then source_id) per this build's task law | B04 (implemented; confirm or veto) | Operator/Research | DEFAULT_IN_FORCE | `order_block_registry.py`; a veto is a tie-break re-derivation, not a new module |
| E15 | F12 has no TTL: RC3's formula row names a generic "TTL" input but no specific PAR governs it (unlike F10's PAR-051); implemented with none (only PENDING→EXPIRED-by-horizon and CONFIRMED→BROKEN are terminal paths) | B04 (implemented; confirm or veto) | Operator/Research | DEFAULT_IN_FORCE | A ratified F12-specific TTL parameter would be a follow-up binding + code change |
| E16 | **Self-caught erratum:** `evidence/receipts/B03.json`'s `contract_manifest_sha256`/`producer_digests.canonical_manifest_hash` (`a66425b3…`) does not match the byte-computed canonical manifest hash of `contracts/manifest/contract_bundle.manifest.b01.v2.json` at the B03 merge commit `1bb00a7b` (independently recomputed, both then and now: `13c4788c7bb3a1a7c9b7802f7d5bf5ac9d473e46a66bae37ccdcaeca4717073e`) — a hand-typed value error at seal time, not a real bundle divergence (no `contracts/` bytes changed between B01R and B04; `verify_manifest.py`/`e2e_audit.py contracts_manifest` both pass against the live file throughout). B03.json is left byte-unchanged (receipts are sealed evidence, corrected forward per the checkpoint-immutability convention, never silently rewritten); the correct value is recorded here for any future cross-check. From B04 onward every receipt's hash fields are computed programmatically from the live artifact (never hand-typed) to close this error class structurally. | B10 audit round (disposition; no code/receipt edit needed) | Build session (self-caught) | CLOSED | No action required on B03.json; B04.json onward computed hashes only |
| E17 | B05's `control/lever_law.py` splits the 32 RC4 refusal codes into three honest reachability classes (`EVENT_LOCAL` — fully implemented in `resolve_manifest`; `STATEFUL_IN_REPO` — implemented in the sibling registry/ledger/health machines against ORIGIN's own owned state; `EXTERNAL_EVIDENCE` — 7 codes [`OFF_TRANSITION_EXPOSURE_REMAINS`, `VENUE_ENVIRONMENT_UNVERIFIED`, `OPPOSITE_ENVIRONMENT_REACHABLE`, `PRIVATE_STATE_STALE`, `UNKNOWN_SUBMIT_UNRECONCILED`] name a fact this repository does not itself observe — open venue exposure, venue-effect reconciliation, opposite-environment reachability, private-state freshness — since that evidence is E08/E09/venue truth, `REFERENCE_ONLY`/`OUT_OF_REPO` per this repo's own CLAUDE.md ownership law). Each EXTERNAL_EVIDENCE code is still fully REGISTERED (code + meaning + minimum_action, drift-locked) and classifiable via `lever_law.classify_external_refusal(code, fired: bool)`, which raises if `fired` is not an explicit bool — the caller (the eventual E08/E09-adjacent runtime, out of this repo's scope) must supply the fact by name, never inferred, never defaulted | B07 (the authority-fact verifier is the natural consumer of the EXTERNAL_EVIDENCE classifier) | Operator/Research | DEFAULT_IN_FORCE | `control/lever_law.py REFUSAL_SCOPE`; `test_lever_law.py::TestExternalEvidence` |
| E18 | B05's `SHADOW_LINEAGE_INCOMPLETE` is reused by `shadow_ledger.py` for two distinct fault shapes RC4's 32-code registry does not separately name: (a) an at-least-once redelivery whose content hash disagrees with the already-stored row for the same `shadow_trade_id` (an identity collision — LEV-0070's "refuse identity collisions with unequal payload hashes") and (b) a `SHADOW_FILL_MODEL` resolution whose `evaluated_at_watermark_us` precedes the frozen candidate's own `market_watermark` (a pre-watermark resolution attempt — LEV-0075). Both are, by the code's own registered meaning ("candidate, rejection, shadow trade, resolver, or outcome lineage is missing or ambiguous"), exactly this: an unresolved/ambiguous claim on one lineage identity. No new refusal code is minted | B05 (implemented; confirm or veto) | Operator/Research | DEFAULT_IN_FORCE | `shadow_ledger.py`; a veto would split `SHADOW_LINEAGE_INCOMPLETE` into named sub-codes, a §1-shaped contracts train this repo does not itself vendor |
| E19 | B05's `PAPER_ORDER`/`PAPER_FILL` unknown-reference refusals (an unregistered `virtual_account_id` or `order_id`) use the registered code `LEVER_REGISTRY_INCOMPLETE` ("a required component or feature is absent, duplicated, unknown, or not digest-bound") rather than minting a PAPER-specific "unknown reference" code — the referenced component is simply absent from the registry of things this ledger knows about, which is exactly that code's declared meaning, mirroring the same reuse decision `lever_registry.py` makes for its own key-set completeness law | B05 (implemented; confirm or veto) | Operator/Research | DEFAULT_IN_FORCE | `paper_ledger.py` |
| E20 | B05's ADR-005 supersession artifact (`docs/governance/ADR-005-SUPERSESSION.md`) is prepared and drift-locked into the e2e walk (stage 20 asserts its presence, its unsigned `COUNTERSIGNED:` line, and that it names `LEV-0001` + the sibling-repo scope boundary) but remains **UNSIGNED** by design — signing is the operator's act, not a build-session one. Until signed, `lever_law.BASELINE_MANIFEST` (`OFF/OFF/OFF/LIVE`) remains the only manifest any B05 machine will accept for a non-empty registry key-set requiring a real digest; no non-OFF activation is representable regardless | B10 (or whenever the operator elects to ratify) | Operator | DEFAULT_IN_FORCE | `docs/governance/ADR-005-SUPERSESSION.md`; mirrors register row A2 |
| E21 | B06's semantic capsule registry (`structures/capsules.py`) binds RC2's five ordinal `CAP01..CAP05` entry-convention parameters to the five ratified RC3 stable semantic capsule IDs EXPLICITLY (by name-match + `formula_refs` cross-reference), never by ordinal position, per the milestone breakdown's own instruction. Four of the five binds are clean (`fvg_displacement_first_touch.v1`→PAR-175, `level_excursion_reclaim_flow_confirmed.v1`→PAR-176, `protected_swing_choch_first_retest.v1`→PAR-177 — all agree by both name AND `formula_refs`; `ob_displacement_bos_first_touch.v1`→PAR-174 agrees by name only, since PAR-174's own `formula_refs` field omits F12). Two rows have NO clean RC2 analogue: `dc_swing_bos_first_retest.v1` has no matching CAP-row at all (no PAR-174..178 entry references the F03+F09 pair its own name names) and consumes only the three generic capsule-independent F18 parameters (PAR-064/172/173); `PAR-178 CAP05_SESSION_ENTRY` (session-anchored entry) has no home among the five ratified capsules and is registered `ORPHANED_LEGACY_PARAMETERS` — unbound to any capsule, never guessed onto one | B06 (implemented; confirm or veto) | Operator/Research | DEFAULT_IN_FORCE | `structures/capsules.py`'s own module docstring carries the full per-row evidence; a veto is a re-binding or a new sixth capsule ratification, not a code defect |
| E22 | B06's `structures/candidate_publisher.py` makes three composition decisions not spelled out verbatim by name in RC3/RC4/the milestone breakdown, each reasoned from this repository's own ownership law (E07 decision/E08 risk/E09 order are `REFERENCE_ONLY`/`OUT_OF_REPO`) rather than guessed: **(a)** every ORIGIN-published candidate forks its SHADOW trade with `origin_disposition="ACCEPTED_NOT_EXECUTED"`, never `"REJECTED"` — ORIGIN has not itself rejected a trading decision (it holds no execution authority to reject FROM), it has simply not yet learned the downstream fate; `REJECTED`/`PROVEN_NO_VENUE_EFFECT` are reserved for a future downstream system feeding its own verdict back through `shadow_ledger.py` directly. **(b)** an untradeable/malformed `PUBLISH_CANDIDATE` (failed contract validation, or any of the 12 re-derived tradeability conjuncts false) is `SHADOW_UNTRADEABLE` — a candidate-shape defect this module refuses to publish — not a rejected trading decision either; the two are kept structurally distinct in the emitted event's own `reason_code`. **(c)** `triad.shadow_trade.v1` requires non-empty `rejection_stage`/`rejection_reason` strings even for this non-rejection disposition; rather than invent a plausible-sounding cause, the module stamps the literal self-describing sentinels `ORIGIN_PUBLICATION_SENTINEL_STAGE`/`_REASON` ("not_a_rejection_this_is_the_mandatory_shadow_fork_of_an_origin_publication"). **(d) SUPERSEDED by row E23:** this row originally also treated an unresolvable `capsule_id`/`trial_id` as a raised wiring defect; a PR review correctly identified that as a poison-envelope risk (one malformed candidate could halt the whole deterministic partition), so it is now routed through the `semantically_valid` tradeability conjunct instead — never a raise. See E23 for the full correction | B06 (implemented; confirm or veto) | Operator/Research | DEFAULT_IN_FORCE | `structures/candidate_publisher.py`'s own module docstring carries the full reasoning; a veto on (a)/(b)/(c) is a disposition/sentinel-string change |
| E23 | **PR #22 review corrections (chatgpt-codex-connector, five findings, all addressed in the same PR):** (1) F18's `candidate_geometry.py` computed `risk_ticks = abs(entry - stop)` AFTER the buffer, so a wrong-side natural-invalidation source (e.g. a LONG source already past entry) could admit with a positive-looking "risk" that was actually directionally invalid; fixed by a new `F18_INVALID_STOP_SIDE` abstention checked BEFORE `abs()` can launder it (LONG requires `stop<entry`, SHORT requires `stop>entry`; the exact-equality boundary is left to the existing `F18_ZERO_RISK` code, unchanged). (2) `candidate_publisher.py`'s `semantically_valid` tradeability flag was set EQUAL to `schema_valid` — the same boolean twice, never an independent semantic check, so a schema-valid-but-geometrically-impossible candidate (`rr_denominator="0"`, a sub-floor RR, `targets=[{}]`) would be marked tradeable; fixed with a dedicated `_semantically_valid` function that recomputes, from the frozen wire fields, the PAR-061 2:1 RR floor (exact cross-multiplication), the directional stop-side invariant, and non-hollow positive-reward targets. (3) a malformed candidate missing `candidate_id` (or `hypothesis_id`) crashed the whole transition — `_require_str` raised before the mandatory SHADOW fork could even run, and `_shadow_candidate_envelope` ALSO direct-subscripted both fields, so the untradeable path itself would have crashed too, leaving NO durable evidence at all for that occurrence (violating the B06 "no candidate path without a durable SHADOW write" law more severely than an ordinary refusal — it produced NO write whatsoever); fixed by reading both fields via `.get()` throughout `_publish`/`_shadow_candidate_envelope`, converting the redelivery-disagreement raise to a named abstention event (`CANDIDATE_ALREADY_PUBLISHED_DISAGREEMENT`, mirroring `shadow_ledger.py`'s own LEV-0070 law), and moving capsule/trial resolution INTO `_semantically_valid` (catching `CapsuleUnavailableError`/`TrialUnavailableError` there rather than letting them propagate) — nothing in `PUBLISH_CANDIDATE` handling can now raise on untrusted CANDIDATE content; a raise is reserved for the publisher's own calling-envelope defects (`event_time_us`, `market_watermark`, the version/quote strings) only. (4) `_semantically_valid` also now checks that the resolved trial's OWN `capsule_semantic_id` equals the candidate's declared `capsule_id` — previously a candidate could be attributed to a preregistered trial of a DIFFERENT capsule, silently corrupting that trial's conjunct/ablation results. (5) `clustering.py`'s F19 "two-or-more matched clusters" case originally REFUSED with `F19_AMBIGUOUS_CLUSTER_MERGE`, reasoning from the plain RC3 formula text's "never re-root" clause alone; the review cited `docs/control/rc3_executable_builder.py`'s `formula_errata` table (an authoritative RC3 correction this repository already vendors, which this build had not fully incorporated), whose exact F19 row states "cross-cluster attachment chooses earliest root deterministically and records aliases" — a stronger, more specific instruction. Corrected: the earliest-root matched cluster (by `(root's availability_us, root_candidate_id)`, the same tie-break the ordinary root-fixing law uses) wins and the occurrence joins it; every OTHER matched cluster is recorded in a new `cluster_aliases` map (never merged, never re-rooted — its own frozen record is untouched) and a new `CLUSTER_ALIASED` event names both the winner and the aliased losers; a new `resolve_canonical_cluster_id` read helper resolves the alias chain, and every future match resolves through it BEFORE comparing so an already-unified pair never re-triggers a spurious attachment | B06 (implemented; confirm or veto) | Operator/Research | DEFAULT_IN_FORCE | `structures/candidate_geometry.py`/`candidate_publisher.py`/`clustering.py` + their test batteries (`TestInvalidStopSide`, `TestSemanticReVerification`, `TestIdentityVerification`, `TestCrossClusterAlias`); a veto on (5) specifically would require re-reading the RC3 errata table as non-authoritative, which the register's own authority order (item 6, "RC3 effective control bundle registries") does not support |
| E24 | **PR #23 review correction (chatgpt-codex-connector):** the B06 receipt's first draft set `emitted_at_us`/`observed_at_us` to 2026-10-13 by mechanically copying a "+30 days per milestone" increment pattern that appeared to hold between the B04 and B05 receipts — but that pattern was itself never a real convention: B00–B03 are genuinely dated 2026-08-09 (the actual session date), B04 is a plausible 5 days later (2026-08-14), and B05's own jump to 2026-09-13 was ALREADY future-dated relative to reality, apparently by the SAME mistake (copying the 30-day `expires_at_us` WINDOW as if it were the inter-milestone time delta). The reviewer correctly named the resulting defect: a receipt whose own metadata says its evidence was not yet observed cannot honestly mark a milestone PASSED. **Fixed:** B06's receipt now records the actual gate-reproduction time (2026-08-09T12:00:00Z, matching this session's real date), with `expires_at_us` a genuine 30-day window from THAT honest instant, and the payload signature recomputed to match. **B00–B05 are left byte-unchanged** (sealed evidence, corrected forward per the E16 precedent, never silently rewritten) — B05's own future-dating is a real, now-named defect in ALREADY-MERGED evidence, not fixed here; a future audit round (B10) should formally assess whether a corrective addendum receipt is warranted for B05, since this repository's receipts are append-only artifacts with no in-place-edit convention for a sealed one. Going forward, every receipt's timestamp must be the genuine time-of-reproduction, never a mechanically-incremented placeholder | B10 audit round (disposition; B05 addendum TBD) | Build session (review-caught) / Operator | DEFAULT_IN_FORCE | `evidence/receipts/B06.json` (corrected); `evidence/receipts/B05.json` (byte-unchanged, the named defect) |
| E25 | **B07 design decisions (three, each reasoned from source material, none guessed):** **(a) `triad.candidate_authority.v1` schema.** No schema existed for this contract anywhere in the estate (`rc3_normative_overlay.json`'s own `wiring_only_contracts` names it as such, and `rc3_effective_control_bundle.json` task `RC3-WOP-... /636` explicitly frames "register schema" as outstanding work). Vendored via `tools/gen_contracts.py`, shaped from `triad.producer_lease.v1`'s existing fields (fencing_token/epoch/issued/expires/revoked/state/signature) plus the W09 task text's named telemetry (active epoch, expiry, renewal, allocation, conflicts) and the two-axis selection `selected_engine_cohort ∈ {ORIGIN_CANDIDATE, LEGACY_COMPARATOR}` (RC3-WOP-001/002's `required_typing`). ORIGIN is verify-only (row A7): the schema's `producer` names the external `governance-lease-issuer`, `consumers` names `origin-authority-fact-verifier`. Contract count 43→44; the four hardcoded counts this changed (`test_registry_lists_all_contracts`, `test_public_contract_views_cannot_mutate_cached_validation_truth`, and `tools/test_wheel_install.py`'s embedded `43`/`130`→`44`/`133` smoke-test literals) were all updated in the same commit. **(b) The 192-row `require()` admit/refuse partition** (`config/parameters.py`). The milestone text names `PROPOSED_*`/`NOT_RATIFIED`/stale/missing/scope-mismatched as refused but does not enumerate the full 15-status vocabulary; every non-`RATIFIED_RC1` status was read individually (its real `declared_value`/`failure_behavior` text, not guessed) and partitioned by a closed ALLOWLIST (never a denylist — an unrecognized future status is a load-time defect, not a silent admit): **refused** (141 rows) = `PROPOSED_RC2_MUST_RATIFY`(123) + `BLOCKING_OWNER_DECISION`(8, `declared_value` literally `NOT_RATIFIED`) + `BLOCKING_RESEARCH_DECISION`(3, same) + `SUPERSEDED`(3, a newer authoritative row exists) + `TARGET_NOT_PROVISIONED`(4, the infra target — JetStream — does not exist to bind to); **materialized** (51 rows) = `RATIFIED_RC1`(21) + `DECLARED_RC2`(8) + `TARGET_FROM_TOPOLOGY`(8, SLO targets, not unresolved decisions) + `CURRENT_VENDOR_REQUIRED`(4) + `DECLARED_RC3`(3, incl. the comparator's own `ENGINE_COHORT_ENUM`/`INTELLIGENCE_ARM_ENUM` rows) + `VENUE_DERIVED`(2, the DERIVATION RULE materializes, never a live venue read) + `RATIFIED_RC3_OVERRIDE`(1) + `RATIFIED_MAKER_LAW`(1) + `DECLARED_RC3_FROM_UNION`(1) + `FORMULA_DERIVED`(1) + `VERIFIED_AS_BUILT_2026_08_07`(1). 141+51=192, verified programmatically. A materializable-status row whose `declared_value` is literally `NOT_RATIFIED` is a defense-in-depth load-time error (never silently downgraded — none exist in the current bundle, verified). **(c) The 13-domain signed configuration bundle** (`config/signed_bundle.py`). The milestone names exactly thirteen domains (capsules, geometry, lifecycle, regime eligibility, symbols, staleness, timing, selected-policy references, transport bindings, compatibility, treatment/rollback, secrets allowlist, exact four-plane manifest) but does not name which parameter rows belong to each. Two are genuinely CONTRACT-shaped, not parameter-shaped, in this repository (`selected_policy_references`→`triad.decision.v2`'s policy fields; `compatibility`→`triad.compatibility_manifest.v1`; `exact_four_plane_manifest`→`triad.engine_control_manifest.v2` + `triad.runtime_lever_registry.v1`, already built in B01/B05) and are referenced by contract id, checked against the live contract registry (an unknown contract id fails the whole bundle build closed). The other ten are anchored to a hand-VERIFIED (name/category/formula_refs cross-read, not guessed) MINIMUM set of parameter ids per domain — explicitly documented as non-exhaustive (this repository's own established law, row E6/E21: "no guessed ... mapping"; an honest partial anchor beats a fabricated complete one). Each domain in the signed bundle names BOTH its materialized members (id+value) and its pending/refused members (id+status) — never a silent omission, mirroring the estate-wide "silence is not a result" law | B07 (implemented; confirm, extend, or veto) | Operator/Research | DEFAULT_IN_FORCE | `contracts/schemas/triad.candidate_authority.v1.schema.json`; `src/triad_origin/config/parameters.py`; `src/triad_origin/config/signed_bundle.py` — each module's own docstring carries the full reasoning; a veto on (c)'s specific anchor set is an additive domain-membership extension, not a defect |
| E26 | **B07 `authority_fact_verifier.py` design decisions (four, each a genuine RC3 W09 reading judgment, documented rather than guessed silently):** RC3 W09's mandatory edge-behavior line reads verbatim "Stale/duplicate/wildcard token rejects; split brain stops new entries" — grammatically ambiguous over WHICH noun "wildcard" modifies, and silent on exactly what makes a duplicate a "split brain" trigger. **(a) WILDCARD is read as wildcard SCOPE, not wildcard token** — `fencing_token` is schema-constrained to `^(0|[1-9][0-9]*)$` (decimal digits only, vendored in E25(a)'s schema), so a token literally cannot contain `"*"`; the ordering law's own text ("wins EXACT scope") only makes sense if scope is the bounded concept, so an empty scope OR any field carrying the literal `"*"` marker refuses as `WILDCARD_SCOPE` (`_is_wildcard_scope`). **(b) DUPLICATE is read as an UNCONDITIONAL refusal at the SAME token** (never a silent idempotent accept) UNLESS the content is byte-identical (pure redelivery) — RC3's own text says "duplicate...rejects" with no stated carve-out, so a byte-identical redelivery is treated as "not really a duplicate claim, a retransmission of the same fact" (mirroring this repository's OWN established redelivery law elsewhere — `journal.register_input`, `shadow_ledger`'s LEV-0070 — RC3 text ranks above repo-pattern-inference per the authority order, but the two readings do not conflict: RC3 refuses a *disagreeing* duplicate, and this repo's precedent independently supports treating identical redelivery as a no-op rather than an error). **(c) A disagreeing same-token duplicate IS the split-brain trigger** (not a separately-defined condition) — two issuers presenting different content under one token is exactly the shape a fencing scheme cannot arbitrate (there is no higher-token tiebreak when tokens are EQUAL), so `admit_fact` latches split-brain at exactly that event, never for merely-competing-but-orderable (different-token) facts, which the ordering law already resolves cleanly. **(d) Readiness is NARROWED to what this module owns** — RC3 W09 names three readiness facts ("activation manifest, producer attestation and allocation current"); `readiness()` checks the two this repository holds (the authority fact's `activation_manifest_id` + the technical-writer lease's currency) and reports the third (`producer_attestation`) as the NAMED STRING `"NOT_OWNED_HERE"` rather than silently assuming it true — a `triad.engine_attestation.v2`-shaped attestation is E08/E09/venue-truth territory (row A7/CLAUDE.md `REFERENCE_ONLY`). A fifth, non-interpretive fix: `revoked_at_us` is a REQUIRED, always-present integer on BOTH `candidate_authority.v1` and `producer_lease.v1` (never nullable — confirmed against the vendored `producer_lease.v1` golden vector, which carries a real-but-not-yet-meaningful `revoked_at_us` alongside `state=ISSUED`), so currency/revocation checks key on `state == "ACTIVE"` alone, never on `revoked_at_us is None` (which can never be true and would have been a permanently-dead check) | B07 (implemented; confirm, extend, or veto) | Operator/Research | DEFAULT_IN_FORCE | `src/triad_origin/control/authority_fact_verifier.py`'s own module docstring carries the full reasoning; a veto on (a)/(b)/(c) is a re-reading of the RC3 W09 edge-behavior line, not a code defect |
| E27 | **PR #24 review corrections (chatgpt-codex-connector, five findings across `authority_fact_verifier.py`/`legacy_bridge.py`/`comparator.py`, all addressed in the same PR — the same drive-to-green posture as row E23):** (1) `verify_candidate_authority`/`verify_producer_lease` schema-validated `signature` only as a non-empty string — never cryptographically authenticated it. Fixed with an OPTIONAL injected `signature_verifier: Callable[[dict], bool] | None` parameter threaded through both verify functions, `AuthorityLedger` (constructor-level), and `readiness()` (two INDEPENDENT parameters, one per fact type, since the two fact types may carry different keys/issuers) — its absence is documented as "structural check only, never silently read as authenticated"; this module still holds no key material itself (row A7's DARK/no-credential law is unchanged — a caller wanting real cryptographic authentication must inject the predicate). (2) `bridge_legacy_record` stored the caller's own mutable `raw_record` dict directly as `legacy_payload` — a post-call mutation of the caller's object could retroactively corrupt an already-bridged envelope. Fixed via a canonical round-trip (`loads_canonical(canonical_json(raw_record))`) at construction, producing a fully detached copy. A second, deeper instance of the SAME class of bug was found while fixing the first: `LegacyBridgeState.bridge()`/`bridged_at()`'s read paths only shallow-copied (`dict(entry)`), so every returned envelope still shared ONE nested `legacy_payload` sub-dict — mutating a RETURNED envelope could corrupt the internally cached state for every future read. Fixed by switching both read paths to `copy.deepcopy` (`copy` is an already-`ALLOWED_MODULE_ROOTS` import). (3) `compare_engine_cohort`/`compare_intelligence_arm` validated only that each PRESENT candidate carried the correct label for ITS OWN axis, never that the two candidates agreed on the axis NOT under comparison — so a divergence attributed to (say) `engine_cohort` could actually be confounded by a simultaneous, unnoticed `intelligence_arm` change between control and treatment. Fixed: when BOTH candidates are present, each function now additionally requires the non-compared axis to be IDENTICAL across control/treatment, raising `ComparatorError` otherwise. This required rebuilding `tests/control/test_comparator.py`'s `_treatment()` fixture default (previously flipped BOTH axes at once — now flips only `engine_cohort`, holding `intelligence_arm` constant by default, since every default-built pair in that file exercises the engine_cohort axis) and fixing `tools/e2e_audit.py` stage 22's inline comparator fixture (which previously carried no `intelligence_arm` field at all on either candidate, so it would have raised unconditionally under the fix). (4) `AuthorityLedger.admit_fact` never checked a fact's own CURRENCY (`is_currently_valid_authority`) before running the stale/duplicate/split-brain ordering logic — so an INACTIVE fact (revoked, not-yet-effective, or expired) carrying a high fencing token could still become "the highest accepted token" for its scope (if admitted as the scope's first fact) and then wrongly out-STALE a later, genuinely active, lower-token fact. Fixed with a new `REFUSED_NOT_CURRENTLY_VALID` refusal, checked immediately after schema/signature verification and BEFORE any stateful ledger logic (including the split-brain latch) — an inactive fact's own invalidity is a property of the fact, independent of ledger state. (5) `readiness()` never checked that the authority fact's or lease fact's own `scope` field matched the REQUESTED `scope` parameter — a fact issued for a completely different scope (e.g. a different instrument/account) could still report `ready=True` for the requested one. Fixed with two new named conjuncts (`authority_scope_matches`/`lease_scope_matches`, exact-equality — never a "broader scope covers it" inference, mirroring the ordering law's own EXACT-scope discipline), folded into `ready`. New tests were added proving each fix closes its named gap (signature-verifier accept/reject + threading on both fact types + the ledger; mutation-after-bridge detachment at both the raw-record and the returned-envelope layers, incl. across idempotent redelivery; cross-axis-contamination refusal on both comparator functions; an inactive high-token fact never wrongly out-staling a later active lower-token fact, incl. under an active split-brain latch; scope-mismatch refusal on both fact types independently) | B07 (implemented; confirm or veto) | Operator/Research | DEFAULT_IN_FORCE | `src/triad_origin/control/{authority_fact_verifier,legacy_bridge,comparator}.py` + their test batteries `tests/control/test_{authority_fact_verifier,legacy_bridge,comparator}.py` + `tools/e2e_audit.py` stage 22's fixture fix; a veto is a re-review of any of the five, not evidence of a build defect, since all five were verified real before fixing |
| E28 | **PR #25 review correction (chatgpt-codex-connector):** the B07 receipt's first draft named an `evidence_sha256s` digest computed from a LOCAL, uncommitted gate-reproduction transcript — a self-reported hash bound to nothing inspectable in the repository, so a future B08/auditor following the receipt could not independently confirm what `PASS` actually attested (the reviewer's exact point: `validate_b_receipt.py` only checks the array is nonempty and the self-integrity signature recomputes, so an arbitrary self-reported result would still validate). **Fixed:** committed the exact transcript the digest was computed from as `evidence/receipts/B07.gate_reproduction.log`, re-hashed the committed file to confirm byte-identity with the already-recorded digest, added a matching `evidence_ids` entry naming the committed path, and re-signed. **A named, NOT-retroactively-fixed gap:** the SAME pattern likely holds for every prior B00–B06 receipt — a direct search for B06's `evidence_sha256s` value (`7a6e8b61f...`) against every file in the repository at merge time finds no match, meaning that digest, too, was never bound to a committed artifact. Per this repository's sealed-evidence convention (row E24: "B00–B05 are left byte-unchanged... never silently rewritten"), those receipts are NOT edited here — this row names the gap for the B10 audit round to formally assess (a corrective addendum receipt per milestone, or a documented acceptance that pre-B07 evidence digests are descriptive-only, is an operator/B10 decision, not a build-session unilateral edit) | B10 audit round (disposition; B00–B06 addendum TBD) | Build session (review-caught) / Operator | DEFAULT_IN_FORCE | `evidence/receipts/B07.json` (corrected, evidence log committed alongside it); `evidence/receipts/{B00,B00C,B01,B01R,B02,B03,B04,B05,B06}.json` (byte-unchanged, the named gap) |

## F · Estate wiring handoffs (Track B)

| Row | Question | Required before | Owner | Status |
|---|---|---|---|---|
| F1 | Host repo/process for the E01 `market_state.v2` bridge (W01/W02) | G5 estate homing | Operator | OPEN |
| F2 | Legacy `candidate.v1` source topic/ledger path to freeze for the control arm | G4 | Operator | OPEN |
| F3 | Divergence/authority topic file-ledger paths for `transport_bindings.v1` | G4 | Operator | OPEN |
| F4 | SHADOW disposition consumer: estate shadow bank vs local durable ledger | G5 | Operator | OPEN — default: local durable ledger, contract-shaped rows |
| F5 | RC4 L1 as-built census of the seven external engines | G5 | Operator | OPEN — confirmed a separate program by default |

## G · Session-scope confirmations

| Row | Question | Required before | Status | Disposition |
|---|---|---|---|---|
| G1 | RC1-complete-edition HTML hash-registered, not vendored | B01 (was) | DEFAULT_IN_FORCE | Hash registration suffices; RC3 embeds RC1 |
| G2 | Receipt cadence | B00C receipt | CLOSED (audit) | The one-behind scheme is **rejected**. One source PR + one post-merge evidence-only receipt PR per milestone; next branch only after the receipt merges and validates. Historical B00/B01 receipts: content retained, cadence disposition recorded (A3) |
