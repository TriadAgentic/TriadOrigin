__pycache__/
*.pyc
*.pyo
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/
build/
dist/
.venv/
venv/
.coverage
htmlcov/
# ORIGIN never commits runtime ledgers, checkpoints or secrets
/run/
/var/
*.ledger
*.checkpoint
secrets/
*.env

# The frozen independent audit runner is deployed BESIDE the repo by the operator,
# never committed (its SHA-256 is pinned in docs/plan/AUDIT_RUNNER_PROVENANCE.md).
audit_package/
